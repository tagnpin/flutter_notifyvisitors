import 'package:flutter/material.dart';

class AppColors {
  // Brand inspired
  static const Color primary = Color(0xFF0067A5); // Blue-600
  static const Color secondary = Color(0xFF6c757d); // Slate-900
  static const Color accent = Color(0xFF22C55E); // Green

  // Light theme
  static const Color lightBackground = Color(0xFFF8FAFC);
  static const Color lightSurface = Colors.white;
  static const Color lightBorder = Color(0xFFE2E8F0);

  // Dark theme
  static const Color darkBackground = Color(0xFF020617);
  static const Color darkSurface = Color(0xFF020617);
  static const Color darkBorder = Color(0xFF1E293B);

  // States
  static const Color error = Color(0xFFEF4444);

  static const Color warning = Color(0xFFF59E0B);
  static const Color success = Color(0xFF10B981);

  // Text colors
  static const Color lightTextPrimary = Color(0xFF0F172A);
  static const Color lightTextSecondary = Color(0xFF64748B);
  static const Color darkTextPrimary = Colors.white;
  static const Color darkTextSecondary = Color(0xFF94A3B8);
}
